<UserControl x:Class="OGV2P.Admin.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             xmlns:infra="clr-namespace:Infrastructure.Converters;assembly=Infrastructure"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid>
        <tk:BusyIndicator IsBusy="{Binding IsBusy}" HorizontalAlignment="Center" VerticalAlignment="Center" DisplayAfter="0" Panel.ZIndex="100">
            <tk:BusyIndicator.BusyContentTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <Image gif:ImageBehavior.AnimatedSource="..\Images\loggingin.gif"  Width="100" Height="100" />
                    </StackPanel>
                </DataTemplate>
            </tk:BusyIndicator.BusyContentTemplate>
            <tk:BusyIndicator.OverlayStyle>
                <Style TargetType="Rectangle">
                    <Setter Property="Fill" Value="#ffffeeee"/>
                </Style>
            </tk:BusyIndicator.OverlayStyle>
            <tk:BusyIndicator.ProgressBarStyle>
                
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </tk:BusyIndicator.ProgressBarStyle>
            <ContentControl />
        </tk:BusyIndicator>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Label>User ID:</Label>
                <TextBox x:Name="txtUserID" Text="{Binding UserID}"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <Label>Password:</Label>
                <TextBox Text="{Binding Password}"></TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right" Margin="0 10 5 0">
                <Button IsDefault="True" Command="{Binding LoginCommand}" Style="{DynamicResource btn-primary}">Login</Button>
            </StackPanel>
            <TextBlock Text="{Binding Message}" 
                       Margin="10 10 10 10"></TextBlock>
            
            <StackPanel Orientation="Vertical" Visibility="{Binding IsReady,Converter={StaticResource BoolToVisibility}}">
                <CheckBox IsChecked="{Binding IsCameraServiceReady}">Camera Servcie Ready</CheckBox>
                <CheckBox IsChecked="{Binding IsFilterServiceReady}">Filter Service Ready</CheckBox>
            </StackPanel>
        </StackPanel>        
    </Grid>
</UserControl>
