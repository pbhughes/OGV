<UserControl x:Class="OGV2P.AgendaModule.Views.AgendaStartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:input="clr-namespace:System.Windows.Input;assembly=System"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="UserControl">
            <Setter Property="Margin" Value="10"/>
        </Style>
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Key="Space" Modifiers="Shift" Command="{Binding StampItem}" CommandParameter="{Binding SelectedItem}" />
    </UserControl.InputBindings>
  
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition  />
                <ColumnDefinition Width="125"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Meeting Name:"/>
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled">
            <TextBox  x:Name="txtMeetingName" 
                          Margin="0 0 5 0" Text="{Binding MeetingName}"/>
        </ScrollViewer>
           
            <Button  Grid.Column="2" Command="{Binding LoadAgenda}" Margin="5 0 5 0"
                        Style="{DynamicResource btn-primary}"
                        CommandParameter="{Binding ElementName=agendaTree}">Choose File</Button>
            <Label Grid.Row="1" Grid.Column="0" Content="Meeting Date:"/>
            <DatePicker Grid.Column="1" Grid.Row="1" SelectedDateFormat="Long"
                                SelectedDate="{Binding MeetingDate}" 
                                VerticalContentAlignment="Center"
                                VerticalAlignment="Center"
                  FirstDayOfWeek="Monday"  />
            <WindowsFormsHost Grid.Row="2" Grid.Column="0" 
                              HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch" 
                                  Grid.ColumnSpan="3" MinHeight="525"
                              Margin="20" x:Name="frmHost"  FontSize="16">
                <wf:TreeView x:Name="agendaTree" 
                             DoubleClick="agendaTree_DoubleClick" />
                
            </WindowsFormsHost>
            <Label Grid.Row="3" Grid.Column="0">Title:</Label>
            <ScrollViewer 
                    Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                    VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="txtTitle" 
                         Text="{Binding SelectedItem.Title}"
                         KeyDown="txtTitle_KeyDown"></TextBox>
            </ScrollViewer>

        <Label Grid.Row="4" Grid.Column="0">Description:</Label>
        <ScrollViewer Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                      VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
            <TextBox Text="{Binding SelectedItem.Description}"
                     x:Name="txtDescription"
                     KeyDown="DescriptionBox_KeyDown" />
        </ScrollViewer>

        <Label Grid.Row="5" Grid.Column="0">Time Stamp:</Label>
        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2">
            <TextBlock VerticalAlignment="Center" Margin="7 0 0 0" Width="150"
                       Text="{Binding SelectedItem.TimeStamp}" >
               
            </TextBlock>
            
        </StackPanel>
    </Grid>
   
</UserControl>
