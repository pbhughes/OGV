<UserControl x:Class="OGV2P.AgendaModule.Views.AgendaStartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:tk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="UserControl">
            <Setter Property="Margin" Value="10"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label>Load Agenda:</Label>
                <TextBox x:Name="txtFileName" Text="{Binding FileName}"></TextBox>
                <Button Command="{Binding LoadAgenda}"
                        Style="{DynamicResource btn-primary}"
                        CommandParameter="{Binding ElementName=agendaTree}">Choose File</Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Meeting Name:</Label>
                <TextBox x:Name="txtMeetingName" Text="{Binding MeetingName}"/>
                <StackPanel Orientation="Horizontal">
                    <Label>Meeting Date:</Label>
                    <DatePicker SelectedDateFormat="Long" SelectedDate="{Binding MeetingDate}"  
                  FirstDayOfWeek="Monday" Width="256" Height="37"  />
                    
                </StackPanel>
            </StackPanel>
            
            <WindowsFormsHost x:Name="frmHost" Width="800" Height="600">
                <wf:TreeView x:Name="agendaTree" />
            </WindowsFormsHost>

            <StackPanel Orientation="Vertical" Width="Auto">
                <StackPanel Orientation="Horizontal" Width="Auto">
                    <Label>Title:</Label>
                    <TextBox Text="{Binding SelectedItem.Title}" ></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Width="Auto">
                    <Label>Description:</Label>
                    <TextBox Text="{Binding SelectedItem.Description}" ></TextBox>
                    
                </StackPanel>
                <Button  Style="{DynamicResource btn-primary}" Command="{Binding StampItem}" 
                             CommandParameter="{Binding SelectedItem}">Stamp</Button>
            </StackPanel>
          
        </StackPanel>
    </Grid>
</UserControl>
