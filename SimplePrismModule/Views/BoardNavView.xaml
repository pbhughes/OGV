<UserControl x:Class="OGV.Admin.Views.BoardNavView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:models="clr-namespace:OGV.Admin.Models"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="2" />
            <Setter Property="Height"  Value="25" />
            <Setter Property="Width"  Value="75" />
            <Setter Property="FontSize" Value="11" />
        </Style>
        
    </UserControl.Resources>
    
    <UserControl.DataContext>
        <models:BoardList x:Name="boardList"/>
    </UserControl.DataContext>
    <Border Margin="2" Padding="2" 
            BorderBrush="LightBlue"
            BorderThickness="1" CornerRadius="6" Background="LightBlue">
        <Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding LogOutCommand}">Log Out</Button>
                <Button Command="{Binding LoadAgendaCommand}">Load Agenda</Button>
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
